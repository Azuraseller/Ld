# Kiểm tra trực quan GIF cuối

`624.gif` và `042.gif` đều hiện ở kích thước 240×240. Nhân vật được giữ nguyên, vùng đen thừa của ảnh dọc đã loại bỏ, nền được đưa về cùng tông xám nhạt và hai khung có cùng hình vuông/góc bo khi hiển thị trong Mimi. Hai GIF đã được tạo với playback chậm hơn theo hệ số 0,75× và upload thành công vào WebDev storage.
